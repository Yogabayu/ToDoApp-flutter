import 'package:flutter/material.dart';
import 'package:get/get.dart';

const padding =
    const EdgeInsets.only(left: 0, right: 16.0, bottom: 20.0, top: 10.0);
const defaultSeparator = SizedBox(height: 20);
const defaultSeparator2 = SizedBox(width: 20);
const paddingCol = EdgeInsets.only(left: 26);
const primaryColor = Colors.blue;

final double height = Get.height;
final double width = Get.width;
